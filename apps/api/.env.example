# Copy this file to `.env` and fill in the values.
# `.env` is gitignored — never commit real credentials.
#
# Shared vars (e.g. DATABASE_URL) live in the repo root `.env.example` /
# `.env` so all workspaces stay in sync. Only vars specific to this app
# go here.

# The Server Port
PORT=8000
